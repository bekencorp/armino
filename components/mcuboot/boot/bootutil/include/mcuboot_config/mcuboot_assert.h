#pragma once

#ifdef assert
#undef assert
#endif
#define assert(arg)                                                 \
    do {                                                            \
    } while(0)
